TODO
- convert integration tests
- unit test coverage in PR (exclude integration tests)
- verify that all imports use .js extension
- verify that all vi.mock use an import()
- vitest should just run unit tests by default, not integration tests
